import { NgModule } from "@angular/core";

// import { HighlightDirective } from "./highlight.directive";

/** This ng module file is no needed anymore after switching to standalong component. */
@NgModule({
  declarations: [
    // HighlightDirective, // no need in declarations after making it standalong
  ],
  exports: [
    // HighlightDirective, // no need in exports after making it standalong
  ]
})
export class SharedModule { }

